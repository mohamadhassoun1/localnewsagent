# LocalNewsAgent Requirements

# Core dependencies
feedparser>=6.0.0          # RSS feed parsing
requests>=2.28.0          # HTTP requests
beautifulsoup4>=4.11.0    # HTML parsing
lxml>=4.9.0               # Fast XML parsing (optional beautifulsoup backend)

# Optional: Local LLM support
gpt4all>=1.3.0            # Local LLM (orca-mini, other models)

# Optional: Headless browser (for dynamic content)
# playwright>=1.40.0        # Browser automation (install: playwright install)

# Development/testing (optional)
# pytest>=7.0.0
# black>=23.0.0
# pylint>=2.17.0
